package se.ltu.ssr.webapp;

import java.util.Date;

import javax.ws.rs.Consumes;
import javax.ws.rs.GET;
import javax.ws.rs.POST;
import javax.ws.rs.Path;
import javax.ws.rs.Produces;
import javax.ws.rs.QueryParam;
import javax.ws.rs.core.Context;
import javax.ws.rs.core.MediaType;
import javax.ws.rs.core.MultivaluedMap;
import javax.ws.rs.core.UriInfo;

//@Path("/ssc/api/basic")
public class Infocom2017testing {
/*	@POST 
	 @Path("/soil_moisture/submit")
	
	public String getSources(@QueryParam("name") String name, @Context UriInfo uriInfo, String content) {
		System.out.println(new Date());
		MultivaluedMap<String, String> queryParams = uriInfo.getQueryParameters(); 
	     String nameParam = queryParams.getFirst("name");
		return "s";
	}
	@GET 
	 
	@Produces(MediaType.TEXT_PLAIN)
	public String getSources1(@QueryParam("name") String name, @Context UriInfo uriInfo, String content) {
		System.out.println(new Date());
		MultivaluedMap<String, String> queryParams = uriInfo.getQueryParameters(); 
	     String nameParam = queryParams.getFirst("name");
		return "s";
	}*/

}
